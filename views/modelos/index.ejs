<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modelos - EANSA</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <div>
        <h3 class="mb-0">Modelos de Computadora</h3>
        <small class="text-muted">Listado de modelos</small>
      </div>
      <div class="d-flex gap-2 align-items-center">
        <a href="/" class="btn btn-outline-secondary">Inicio</a>
        <% if (user && user.rol === 'administrador') { %>
          <a href="/modelos/nuevo" class="btn btn-primary"><i class="fas fa-plus me-2"></i>Agregar Modelo</a>
        <% } %>
      </div>
    </div>

    <div class="mb-3">
      <% if (typeof user !== 'undefined' && user) { %>
        <small class="text-muted">Usuario: <strong><%= user.nombre %></strong> | rol: <strong><%= user.rol %></strong></small>
      <% } else { %>
        <small class="text-danger">No hay usuario en la vista (no autenticado)</small>
      <% } %>
    </div>

    <div class="row">
      <% modelos.forEach(m => { %>
        <div class="col-md-4 mb-3">
          <div class="card h-100">
            <% if (m.imagen) { %>
              <img src="<%= m.imagen %>" class="card-img-top" style="height:180px; object-fit:cover;" />
            <% } %>
            <div class="card-body">
              <h5 class="card-title"><%= m.nombre %></h5>
              <p class="card-text">Código: <strong><%= m.codigo %></strong></p>
              <div class="d-flex gap-2 flex-wrap">
                <a href="/modelos/<%= m._id %>" class="btn btn-sm btn-outline-primary">Ver historial</a>
                <% if (user && (user.rol === 'administrador' || user.rol === 'moderador')) { %>
                  <a href="/mantenimientos/nuevo?modelo=<%= encodeURIComponent(m.nombre + ' (' + m.codigo + ')') %>&tipo=<%= m.tipo %>&codigo=<%= encodeURIComponent(m.codigo) %>" class="btn btn-sm btn-outline-success">Agregar mantenimiento</a>
                <% } %>
                <% if (user && user.rol === 'administrador') { %>
                  <form method="POST" action="/modelos/<%= m._id %>?_method=DELETE" class="d-inline" onsubmit="return confirm('¿Estás seguro de eliminar este modelo?')">
                    <button type="submit" class="btn btn-sm btn-outline-danger">
                      <i class="fas fa-trash"></i>
                    </button>
                  </form>
                <% } %>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>