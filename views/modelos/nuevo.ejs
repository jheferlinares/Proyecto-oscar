<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nuevo Modelo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <h3>Agregar modelo de computadora</h3>
    <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/modelos" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label class="form-label">Nombre del modelo</label>
        <input type="text" name="nombre" class="form-control" required value="<%= data && data.nombre ? data.nombre : '' %>">
      </div>
      <div class="mb-3">
        <label class="form-label">Código único de almacén</label>
        <input type="text" name="codigo" class="form-control" placeholder="Ej: PC-001, LT-002" required value="<%= data && data.codigo ? data.codigo : '' %>">
      </div>
      
      <div class="row">
        <div class="col-md-6">
          <div class="mb-3">
            <label class="form-label">Procesador</label>
            <input type="text" name="procesador" class="form-control" placeholder="Ej: Intel Core i5-12400, AMD Ryzen 5 5600X" value="<%= data && data.procesador ? data.procesador : '' %>">
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label class="form-label">Tipo de RAM</label>
            <select name="tipoRam" class="form-select">
              <option value="">Seleccionar...</option>
              <option value="DDR3" <%= data && data.tipoRam === 'DDR3' ? 'selected' : '' %>>DDR3</option>
              <option value="DDR4" <%= data && data.tipoRam === 'DDR4' ? 'selected' : '' %>>DDR4</option>
              <option value="DDR5" <%= data && data.tipoRam === 'DDR5' ? 'selected' : '' %>>DDR5</option>
            </select>
          </div>
        </div>
      </div>
      
      <div class="row">
        <div class="col-md-6">
          <div class="mb-3">
            <label class="form-label">Memoria RAM</label>
            <input type="text" name="memoriaRam" class="form-control" placeholder="Ej: 8GB, 16GB, 32GB" value="<%= data && data.memoriaRam ? data.memoriaRam : '' %>">
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label class="form-label">Disco Duro</label>
            <input type="text" name="discoDuro" class="form-control" placeholder="Ej: SSD 256GB, HDD 1TB, SSD 512GB + HDD 1TB" value="<%= data && data.discoDuro ? data.discoDuro : '' %>">
          </div>
        </div>
      </div>
      
      <div class="mb-3">
        <label class="form-label">Tarjeta Gráfica</label>
        <input type="text" name="tarjetaGrafica" class="form-control" placeholder="Ej: Integrada Intel UHD, NVIDIA GTX 1650, AMD Radeon RX 6600" value="<%= data && data.tarjetaGrafica ? data.tarjetaGrafica : '' %>">
      </div>
      
      <div class="mb-3">
        <label class="form-label">Imagen del modelo (opcional)</label>
        <input type="file" name="imagen" accept="image/*" class="form-control">
      </div>
      <button class="btn btn-primary">Crear modelo</button>
    </form>
  </div>
</body>
</html>